name: asistencia_movil
description: "UTB Assists - Sistema de Asistencias UTB"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.0
  jose: ^0.3.2
  dotenv: ^4.2.0
  flutter_dotenv: ^6.0.0
  jwt_decoder: ^2.0.1
  flutter_web_auth_2: ^4.1.0
  fl_chart: ^1.1.1
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/uni-logo.png
    - assets/foto-estudiante.jpg
